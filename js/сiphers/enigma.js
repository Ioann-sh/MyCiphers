function enigmaEncrypt(text, rotor1, rotor2, rotor3, reflector, initialPositions) {
    return text
}

function enigmaDecrypt(text, rotor1, rotor2, rotor3, reflector, initialPositions) {
    return text
}

function enigmaHack(text) {
    return text
}